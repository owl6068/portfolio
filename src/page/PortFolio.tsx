import { Inner, Section } from "../css/common";
import { IPageId } from "../utils/interface";

function PortFolio({ id }: IPageId) {
  return (
    <Section id={id}>
      <Inner>
        <div>asd</div>
      </Inner>
    </Section>
  );
}

export default PortFolio;
